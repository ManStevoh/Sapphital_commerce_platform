{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Terminal Tech Theme Settings",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "primary_color": {
      "type": "string",
      "title": "Primary colour",
      "description": "Brand primary colour (hex). Default: deep navy.",
      "pattern": "^#[0-9A-Fa-f]{6}$",
      "default": "#0D1B2A"
    },
    "font_heading": {
      "type": "string",
      "title": "Heading font",
      "description": "Font family for headings and store name.",
      "enum": ["Inter", "DM Sans", "Playfair Display", "Source Sans 3"],
      "default": "Inter"
    },
    "logo_url": {
      "type": ["string", "null"],
      "title": "Logo URL",
      "description": "Merchant logo served via CDN. Null uses store name text.",
      "format": "uri",
      "default": null
    }
  },
  "required": ["primary_color", "font_heading"]
}
